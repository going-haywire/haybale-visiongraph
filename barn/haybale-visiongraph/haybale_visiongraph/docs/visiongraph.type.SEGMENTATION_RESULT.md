# Segmentation Result

`visiongraph:type:SEGMENTATION_RESULT` · kind: type

Instance segmentation: detection plus a per-instance mask

## Details

- **flow_type**: `data`
- **default**: `{'results': []}`
- **color**: `#7b1fa2`

## Notes

``ResultList[InstanceSegmentationResult]`` — a detection plus a binary mask
for the segmented region.
